<h3>Formulaire</h3>

<div class="well">
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    
    {{ form_errors(form) }}

        <div class="form-group">
       {{ form_row(form.Enregistrer, { 'label': 'Enregistrer' }) }}
    </div>
    
    
    <h4>Identité</h4>
    <div class="form-group">
      {{ form_label(form.firstName, "Prénom", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
      {{ form_errors(form.firstName) }}
      <div class="col-sm-4">
        {{ form_widget(form.firstName, {'attr': {'class': 'form-control'} }) }}
      </div>
    </div>
    
    <div class="form-group">
      {{ form_label(form.name, "Nom", {'label_attr': {'class': 'col-sm-3 control-label'} }) }}
      {{ form_errors(form.firstName) }}
      <div class="col-sm-4">
        {{ form_widget(form.name, {'attr': {'class': 'form-control'} }) }}
      </div>
    </div>
    
    <div class="form-group">
        {{ form_label(form.name, "Description / Lien de parenté", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.description) }}
      <div class="col-sm-4">
        {{ form_widget(form.description, {'attr': {'class': 'form-control'} }) }}
      </div>
    </div>
    
    <div class="form-group">
        {{ form_label(form.anitasFamily, "Appartient à la famille d'Anita", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.anitasFamily) }}
        <div class="col-sm-1">
            {{ form_widget(form.anitasFamily, {'attr': {'class': 'form-control'} }) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.georgesFamily, "Appartient à la famille de Georges", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.georgesFamily) }}
        <div class="col-sm-1">
            {{ form_widget(form.georgesFamily, {'attr': {'class': 'form-control'} }) }}
        </div>
    </div>
    
    <div class="form-group">
        {{ form_label(form.gender, "Genre", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.gender) }}
        <div class="col-sm-3">
            {{ form_widget(form.gender, {'attr': {'class': 'form-control'} }) }}
        </div>
    </div>
    
    <div class="form-group">
        {{ form_label(form.dateOfBirth, "Né(e) le", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.dateOfBirth) }}
        {{ form_widget(form.dateOfBirth, {'attr': {'class': 'col-sm-3'} }) }}
    </div>
    
    <div class="form-group">
      {{ form_label(form.placeOfBirth, "à", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
      {{ form_errors(form.placeOfBirth) }}
      <div class="col-sm-4">
        {{ form_widget(form.placeOfBirth, {'attr': {'class': 'form-control'} }) }}
      </div>
    </div>

    <div class="form-group">
        {{ form_label(form.dateOfDeath, "Décédé(e) le", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        {{ form_errors(form.dateOfDeath) }}
        {{ form_widget(form.dateOfDeath, {'attr': {'class': 'col-sm-3'} }) }}
    </div>
    
    <div class="form-group">
      {{ form_label(form.placeOfDeath, "à", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
      {{ form_errors(form.placeOfDeath) }}
      <div class="col-sm-4">
        {{ form_widget(form.placeOfDeath, {'attr': {'class': 'form-control'} }) }}
      </div>
    </div>  
       
    <hr>
    
    <div class="container">
        <h4>Ajouter un avatar</h4>
    <!--    <div class="form-group">
          {# form_label(form.image.path, "Chemin", {'label_attr': {'class': 'col-sm-3 control-label'}}) #}
          {# form_errors(form.image.path) #}
          <div class="col-sm-4">
            {# form_widget(form.image.path, {'attr': {'class': 'form-control'} }) #}
          </div>
        </div>
        
        
        <div class="form-group">
          {# form_label(form.image.name, "Nom du fichier + format", {'label_attr': {'class': 'col-sm-3 control-label'#}) #}
          {# form_errors(form.image.name) #}
          <div class="col-sm-4">
            {# form_widget(form.image.name, {'attr': {'class': 'form-control'} }) #}
          </div>
        </div>
        
        <div class="form-group">
          {# form_label(form.image.alt, "Alt", {'label_attr': {'class': 'col-sm-3 control-label'} }) #}
          {# form_errors(form.image.alt) #}
          <div class="col-sm-4">
            {# form_widget(form.image.alt, {'attr': {'class': 'form-control'} }) #}
          </div>
        </div>
    </div> -->
    
     

    
    {{ form_end(form) }}

</div>

{#############################}

{# On charge la bibliothèque jQuery. Ici, je la prends depuis le CDN google mais si vous l'avez en local, changez simplement l'adresse. #}
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

<script type="text/javascript">
  $(document).ready(function() {
    // On récupère la balise <div> en question qui contient l'attribut « data-prototype » qui nous intéresse.
    var $container = $('div#oph_familytreebundle_person_listPhotos');

    // On ajoute un lien pour ajouter une nouvelle catégorie
    var $addLink = $('<a href="#" id="add_listPhotos" class="btn btn-default">Ajouter une photo</a>');
    $container.append($addLink);

    // On ajoute un nouveau champ à chaque clic sur le lien d'ajout.
    $addLink.click(function(e) {
      addPhotoToGallery($container);
      e.preventDefault(); // évite qu'un # apparaisse dans l'URL
      return false;
    });

    // On définit un compteur unique pour nommer les champs qu'on va ajouter dynamiquement
    var index = $container.find(':input').length;

    // On ajoute un premier champ automatiquement s'il n'en existe pas déjà un (cas d'une nouvelle annonce par exemple).
    if (index == 0) {
      addPhotoToGallery($container);
    } else {
      // Pour chaque catégorie déjà existante, on ajoute un lien de suppression
      $container.children('div').each(function() {
        addDeleteLink($(this));
      });
    }

    // La fonction qui ajoute un formulaire Categorie
    function addPhotoToGallery($container) {
      // Dans le contenu de l'attribut « data-prototype », on remplace :
      // - le texte "__name__label__" qu'il contient par le label du champ
      // - le texte "__name__" qu'il contient par le numéro du champ
      var $prototype = $($container.attr('data-prototype').replace(/__name__label__/g, 'Photo n°' + (index+1))
          .replace(/__name__/g, index));

      // On ajoute au prototype un lien pour pouvoir supprimer la catégorie
      addDeleteLink($prototype);

      // On ajoute le prototype modifié à la fin de la balise <div>
      $container.append($prototype);

      // Enfin, on incrémente le compteur pour que le prochain ajout se fasse avec un autre numéro
      index++;
    }

    // La fonction qui ajoute un lien de suppression d'une catégorie
    function addDeleteLink($prototype) {
      // Création du lien
      $deleteLink = $('<a href="#" class="btn btn-danger">Supprimer</a>');

      // Ajout du lien
      $prototype.append($deleteLink);

      // Ajout du listener sur le clic du lien
      $deleteLink.click(function(e) {
        $prototype.remove();
        e.preventDefault(); // évite qu'un # apparaisse dans l'URL
        return false;
      });
    }
  });
</script>