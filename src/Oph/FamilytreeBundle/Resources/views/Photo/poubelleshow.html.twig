{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Photo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datephoto</th>
                <td>{{ entity.datePhoto|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_photo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_photo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
